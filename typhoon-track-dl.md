# typhoon-track-dl

気象庁ベストトラックデータを用いて、東アジアにおける台風進路の月毎の傾向を
深層学習（自己組織化マップ等）で探究するプロジェクト。

このファイルは pc_docs/manuals/automation/typhoon-track-dl.md のコピーです
（詳細は [README.md](README.md) / [MANUAL.md](MANUAL.md) を参照）。

## 概要

- 気象庁RSMC東京の台風ベストトラックデータ（緯度・経度・中心気圧・最大風速の6時間毎時系列、1951年〜現在）を使用。
- 「進路図の画像」ではなく、公式に構造化された数値データ（緯度経度時系列）を直接扱う方針。
  画像をCNNで読ませて座標を逆算するアプローチは不要な遠回りと判断し不採用。
- Self-Organizing Map（SOM）による教師なしクラスタリングで進路パターンを分類し、
  月ごとの出現傾向を可視化することが本命のアプローチ。

## 進め方（段階的）

1. **Phase0 データ取得・前処理**: IBTrACS（NOAA、JMA報告分を含む整形済みCSV）または
   気象庁ベストトラックテキストを取得し、台風ごとの緯度経度時系列を抽出・月別グループ化。
2. **Phase1 古典的検証**: 月別に全トラックを地図に重ね書きし、既知の季節傾向
   （夏は日本近海で北上、秋は南下してから再カーブ等）と整合するか目視・統計で確認。
3. **Phase2 SOMクラスタリング（本命）**: `MiniSom`等でCPUのみ学習。
   台風進路分類の研究実績がある手法。各ノード＝代表的な進路パターンとして解釈し、月別集計。
4. **Phase3（発展）**: LSTM系列オートエンコーダとの比較検証。

## 実行環境の見積もり

- 総台風数はおよそ1800〜2000個（1951年〜現在、年25〜30個）。
- SOM学習はMacBook Pro（CPU）で数分〜数十分程度の見込み。GPU不要。

## 注意事項

- データダウンロードに `curl` / `wget` は使用しない。Pythonの `requests` 等を使うこと。
- 気象庁データの利用規約を遵守する。

## Phase0実装（データ取得・前処理）

データソースは気象庁公式ベストトラックテキスト（IBTrACSではなくこちらを採用。方針
「公式の構造化データを直接使う」により忠実なため）。

- `src/download.py`: `requests` で全期間データzip
  （`https://www.jma.go.jp/.../Besttracks/bst_all.zip`）を取得・展開。
- `src/parse.py`: 固定長テキストをパースし観測点単位のCSV
  （`data/processed/best_track.csv`）を生成。

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python src/download.py
.venv/bin/python src/parse.py
```

フォーマット仕様（桁位置）は公式仕様書の記載と実データで軽微な食い違いがあったため、
実データ照合で確定させたもの。詳細は [MANUAL.md](MANUAL.md) の
「Phase0実装メモ」を参照。

検証: HAGIBIS（国際番号1919、令和元年東日本台風）の最盛期記録
（中心気圧915hPa・最大風速105kt）が気象庁公表値と一致することを確認。

## Phase1実装（古典的検証・可視化）

`src/visualize_monthly.py`: 台風の発生月ごとにグループ化し、`cartopy`で
東アジア地図（経度100-180°、緯度0-50°）に全トラックを半透明線で重ね書きした
12パネル図（`outputs/phase1_monthly_tracks.png`）を生成。

```bash
.venv/bin/python src/visualize_monthly.py
```

結果は既知の季節傾向とよく整合:
- 7-8月（台風数最多期）は日本近海へ直進する経路が密集
- 9-10月はフィリピン近海から偏西風帯に入り東へ再カーブする経路が明瞭に増加
- 11-3月（台風数最少期）は発生位置が低緯度側にシフトし南シナ海方面へ向かう経路が主流

## ステータス

2026-07-26 Phase1（古典的検証）完了。月別台風進路の可視化が既知の季節傾向と
整合することを確認。データ取得・パースの妥当性が裏付けられ、Phase2（SOMクラスタリング）
に進める見込み。

2026-07-26 Phase0（データ取得・前処理）実装完了。台風1955個・観測点71223件の
パース済みCSVを生成できることを確認。
