# typhoon_track_dl — 手法・進め方の詳細

東アジアにおける台風進路の月別傾向を、深層学習的アプローチで探究するプロジェクト。

## 前提の見直し：「画像」ではなく構造化データを使う

気象庁は台風の経路を**ベストトラック（RSMC Tokyo Best Track Data）**として、
1951年以降の全台風について6時間ごとの緯度・経度・中心気圧・最大風速等を
**テキスト形式（固定長フォーマット）**で公開している。

- 公式配布: 気象庁 RSMC東京－台風センター ベストトラックデータ
- 代替: [IBTrACS](https://www.ncei.noaa.gov/products/international-best-track-archive)（NOAA）
  にJMA報告分が既に整形済みCSV/NetCDFで含まれており、パース済みで扱いやすい。

**画像（進路図の絵）をCNNに読ませて緯度経度を逆算する**アプローチは、
既に構造化された数値データが公式に存在する以上、遠回りかつ不要な誤差要因になる。
本プロジェクトでは**緯度経度の時系列データを直接使う**方針とする。
「ベストトラック画像」という表現は、実質的に「ベストトラックデータによる進路」を指すものとして進める。

## 段階的な進め方（できるか不明なため、小さく検証しながら進める）

### Phase 0: データ取得・前処理
- IBTrACS（西太平洋バシン）または気象庁ベストトラックテキストを取得。
- 台風ごとに緯度・経度の時系列（6時間毎）を抽出。
- 発生月（または各地点の月）でグループ化。
- トラック長（点数）が台風ごとに異なるため、等間隔リサンプリング等で系列長を揃える。

### Phase 1: 古典的検証（深層学習の前に、まず目視・統計で妥当性確認）
- 月別に全トラックを東アジア地図に重ね書きし、既知の季節傾向
  （例: 7-8月は日本近海で北上しやすい、9-10月は偏西風帯まで南下してから
  再カーブする経路が増える等）と整合するか確認する。
- ここで明らかな傾向が見えない場合、データ処理か仮説自体を見直す。

### Phase 2: SOM（自己組織化マップ）によるパターン分類（本命）
- Self-Organizing Map は教師なしニューラルネットワークの一種で、
  台風進路の分類研究で実績がある手法（例: Kim et al. 2011, Nakamura et al. 2009 等、
  西太平洋台風のSOM分類研究）。
- `MiniSom`（軽量・numpyベース・GPU不要）等のライブラリでCPUのみで学習可能。
- 学習後、各SOMノード＝代表的な進路パターンとして解釈できる
  （直進型・再カーブ型・停滞型など）。
- 台風ごとにどのノードに分類されたかを月別に集計し、
  「月ごとにどの進路パターンが多いか」を定量的に示す。

### Phase 3（発展・余力があれば）: 系列オートエンコーダとの比較
- LSTM/GRUベースの系列オートエンコーダで潜在表現を学習し、
  その潜在空間でk-means等のクラスタリングを行う。
- Phase2のSOM分類結果と比較し、手法によらず一貫した傾向が出るか検証する。

### 可視化
- 月別・SOMノード別の代表経路を東アジア地図上にプロット。
- 月ごとのパターン比率を積み上げ棒グラフ等で表示。

## データ規模・実行環境の見積もり

- 年間約25〜30個、1951年〜現在で総数およそ1800〜2000個の台風。
- SOM学習はこの規模であればCPU（MacBook Pro）で数分〜数十分程度で完了する見込み。
  GPUは不要。
- Phase3のLSTMオートエンコーダも同規模データであればCPUで十分現実的。

## 使用予定ライブラリ

- `pandas` / `numpy`: データ整形
- `matplotlib` + `cartopy`（地図投影）: 可視化
- `minisom`: SOM実装
- `torch` または `tensorflow`（CPU版）: Phase3のオートエンコーダ用（必要になった場合のみ導入）

## 注意事項

- データダウンロードに `curl` / `wget` は使用しない（グローバルCLAUDE.mdのセキュリティルールにより禁止）。
  Pythonの `requests` ライブラリ等で取得すること。
- 気象庁ベストトラックデータの利用規約を遵守すること。

## Phase0実装メモ：ベストトラックテキストのフォーマット仕様

出典: https://www.jma.go.jp/jma/jma-eng/jma-center/rsmc-hp-pub-eg/Besttracks/bst_all.zip
（公式フォーマット仕様書 `e_format_bst.html` の桁位置記載と実データに軽微な食い違いが
あったため、以下は実データ（`data/raw/bst_all.txt`）に対する目視照合で確定させたもの）

**ヘッダー行**（`66666` 始まり、1行=1台風）:

| フィールド | 桁位置(1-indexed) | 内容 |
|---|---|---|
| インジケータ | 1-5 | `66666` |
| 国際番号ID | 7-10 | 下2桁=年、下2桁=その年の通し番号（例: `1919`=2019年19号=HAGIBIS） |
| データ行数 | 13-15 | 続くデータ行の数 |
| 台風名 | 31-50 | 右詰め、命名前は空白 |
| 最終更新日 | 65-72 | yyyymmdd |

**データ行**（1行=6時間毎の1観測点）:

| フィールド | 桁位置(1-indexed) | 内容 |
|---|---|---|
| 日時 | 1-8 | yymmddhh（UTC） |
| グレード | 14 | 強度階級（下表） |
| 緯度 | 16-18 | 0.1度単位 |
| 経度 | 20-23 | 0.1度単位（日付変更線超えは180度超の値も取り得る） |
| 中心気圧 | 25-28 | hPa |
| 最大風速 | 34-36 | kt（1977年以降のみ記録。`000`＝未記録） |

グレードコード対応表:

| コード | 略称 | 意味 |
|---|---|---|
| 2 | TD | 熱帯低気圧 |
| 3 | TS | 熱帯低気圧(TS) |
| 4 | STS | 強い熱帯低気圧 |
| 5 | TY | 台風 |
| 6 | L | 温帯低気圧 |
| 7 | - | 東京責任域への進入時 |
| 9 | - | TS以上（風速データ整備以前の簡易分類） |

2桁年→4桁年の変換は pivot=50（`yy>=51` なら `19yy`、`yy<=50` なら `20yy`）。
本データの対象期間が1951年〜のため、このルールで一意に変換できる。

検証: HAGIBIS（国際番号1919、令和元年東日本台風）の最盛期記録が
中心気圧915hPa・最大風速105ktとしてパースされ、気象庁公表値と一致することを確認済み。

## Phase1実装メモ：月別可視化

`src/visualize_monthly.py` で、台風ごとの発生月（最初の観測点の月）でグループ化し、
`cartopy` で東アジア地図（経度100-180°、緯度0-50°）に月別・全台風のトラックを
半透明の線（`alpha=0.15`）で重ね書きした12パネル図を生成する
（`outputs/phase1_monthly_tracks.png`）。半透明にすることで、経路が集中する
帯（＝その月に多いパターン）が濃い線として浮かび上がる。

日本語フォントは matplotlib の `rcParams["font.family"]` に `"Hiragino Sans"`
（macOS標準搭載）を指定して文字化けを回避。

結果は既知の季節傾向と整合することを確認済み（詳細は README.md 参照）。
これによりデータ取得・パースの妥当性が改めて裏付けられたため、
Phase2（SOMクラスタリング）に進む。
