# CLAUDE.md — typhoon_track_dl

## プロジェクト概要
気象庁ベストトラックデータを使い、東アジアの台風進路の月別傾向を深層学習的手法
（Self-Organizing Map等）で探究する研究・探索的プロジェクト。
詳細は [README.md](README.md) / [MANUAL.md](MANUAL.md) を参照。

## 方針
- 「進路図の画像」をCNNに読ませるのではなく、気象庁ベストトラックの構造化された
  緯度経度時系列データを直接使う（[MANUAL.md](MANUAL.md)参照）。
- 実現可能性が未知数のため、いきなり深層学習に飛びつかず、
  Phase0（データ整形）→ Phase1（古典的可視化での妥当性確認）→
  Phase2（SOMクラスタリング）→ Phase3（オートエンコーダ、発展）の順で段階的に進める。
- 各Phaseの結果は、既知の気象学的知見（季節による再カーブ傾向の変化等）と
  整合するかどうかを都度確認する。

## 技術スタック
- Python（pandas / numpy / matplotlib / cartopy / minisom）
- 発展フェーズでtorch等のCPU版を使う可能性あり（必要になってから導入）

## 注意事項
- データダウンロードに `curl` / `wget` は使用しない（グローバルCLAUDE.mdの禁止事項）。
  Pythonの `requests` 等を使うこと。
- 気象庁データの利用規約を遵守する。
- 大規模な学習・長時間実行になりそうな場合は、実行前にユーザーに確認する。

## コーディング規約
- コメントは日本語で記述
- インデント: スペース4つ（Python）
- ファイル名: スネークケース

## GitHub更新ルール
- ファイルを変更・追加した場合は、必ず `git add` → `git commit` → `git push` までを行うこと（push前にユーザー確認）
